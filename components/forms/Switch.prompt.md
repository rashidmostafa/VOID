Use `Switch` when the change applies instantly — listing visibility, Bengali interface, vendor holiday mode.

```jsx
<Switch label="Listing live" checked onChange={fn} />
<Switch size="sm" label="বাংলা" />
```

If the change needs confirming, use `Checkbox` plus a Save button instead.
