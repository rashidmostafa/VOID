/* Next normally supplies this through its own type generation; declaring it
   explicitly keeps `next build` working when the app is built from the
   workspace root rather than from apps/web. */
declare module "*.css";
