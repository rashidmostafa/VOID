Use `Checkbox` for independent options and table row selection.

```jsx
<Checkbox label="Also list in India" description="Prices convert at the daily rate" checked onChange={fn} />
<Checkbox indeterminate label="Select all" />
```

16px box, `--radius-sm`, fills `--primary` when checked with a 1.6px white tick.
