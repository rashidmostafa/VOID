Use `Tag` for facet filters, size/colour choices and applied-filter chips.

```jsx
<Tag selected onClick={toggle}>Kurta</Tag>
<Tag removable onRemove={clear}>Under ৳3,000</Tag>
```

Fully rounded (`--radius-full`), 28px tall — the only fully rounded shape in the system besides avatars and switches.
