Use `Tooltip` on icon-only controls.

```jsx
<Tooltip label="Mirror artwork"><IconButton icon={<Icon name="flip-horizontal" />} label="Mirror" /></Tooltip>
```

Inverted surface (`--primary`), 11px medium, one line. Two words is the target; a sentence means the control needs a visible label.
